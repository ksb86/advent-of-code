const path = require('path');
const { getInput, newLineString, numArraySum, validateAnswer } = require('../../helpers');

const groups = getInput(path.resolve(__dirname, './input'), { splitTwoLines: true });
// console.log(validateAnswer(1, result));
